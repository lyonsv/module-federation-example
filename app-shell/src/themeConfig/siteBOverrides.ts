import { Theme } from '@mfe-poc/shared';
import { baseTheme } from './baseTheme';

export const siteBTheme: Theme = {
  ...baseTheme,
  primaryColor: '#ea4335',
  secondaryColor: '#fbbc04',
  logoUrl: '/siteB-logo.png',
  fontFamily: 'Open Sans, sans-serif'
}; 